#include "search.h"

/*
std::vector<std::vector<int>> search(struct field field)
{
	struct agent {	//エージェントの位置
		int x, y;
	}a1, a2;

	a1.x = field.a1.x;
	a1.y = field.a1.y;
	a2.x = field.a2.x;
	a2.y = field.a2.y;

	for (int i = -1; i <= 1; i++) {
		for (int j = -1; j <= 1; j++) {

		}
	}
}
*/

int value(struct field field)
{
	int value = 0;


	return value;
}